# Project-local Codex configuration for AI Quality Bootstrap.
#
# Role-specific custom agents will be added later under .codex/agents/.

model_reasoning_effort = "high"

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800

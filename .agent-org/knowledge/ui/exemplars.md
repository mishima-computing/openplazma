# UI Exemplars

This index records date-pinned corpus pointers only. Entries are examples for named UI composition patterns, not evidence excerpts or adoption claims.

Capture-verification rule: exemplar PNGs live under `.agent-runs/<run_id>/` and remain ephemeral packet evidence; this registry stays the durable date-pinned pointer list. An exemplar graduates from owner-recorded or fetch-verified to capture-verified only when a controller-executed `scripts/capture-screens.py` run records a PNG path under the run directory and the packet metadata names the source pointer. Structural check: acceptance evidence cites the run-dir PNG path, not this registry line.

- https://tailscale.com@2026-06-12 -> proof-artifact-density-per-view (fetch-verified; section-type variety, logo wall, metric showcase)
- https://www.deepseek.com/en@2026-06-12 -> one-lead-language-per-view (fetch-verified; pinned to `/en`; root path diverges, never cite bare domain)
- https://www.antgroup.com@2026-06-12 -> corporate-trust-register (owner-recorded; JS-rendered; unverified-by-fetch and unverified-by-capture until a controller-executed PNG lands under a run directory)
